#!/bin/bash
set -eu -o pipefail

stack test --pedantic

[ `uname -s` == linux ] \
  || eval $(docker-machine env default)
time                      \
  STACK_OFFLINE_TEST=FULL \
    stack test stack-offline:test
